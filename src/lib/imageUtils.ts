export const hikeImage = (title: string) => {
  // Placeholder logic, replace with actual image logic if needed
  return `https://via.placeholder.com/400x300?text=Hike+${encodeURIComponent(title)}`;
};

export const courseImage = (title: string, category: string) => {
  // Placeholder logic, replace with actual image logic if needed
  return `https://via.placeholder.com/400x300?text=Course+${encodeURIComponent(title)}+${encodeURIComponent(category)}`;
};